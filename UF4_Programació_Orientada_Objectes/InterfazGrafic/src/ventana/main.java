package ventana;

public class main {
    public static void main(String[] args) {
        Ventana v1 = new Ventana();

        // Hacer visible la ventana ya que por defecto no lo es (false), entonces le ponemos true
        v1.setVisible(true);
    }
}
